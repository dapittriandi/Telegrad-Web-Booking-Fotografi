{
  "$schema": "https://schema.railpack.com",
  "provider": "php",
  "build": {
    "commands": [
      "composer install --no-dev --optimize-autoloader",
      "npm install",
      "npm run build",
      "mkdir -p storage/framework/{sessions,views,cache,testing} storage/logs bootstrap/cache",
      "chmod -R a+rw storage"
    ]
  },
  "deploy": {
    "startCommand": "php artisan config:clear && php artisan view:clear && php artisan migrate --force && php artisan db:seed --force --class=WebSettingSeeder && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}